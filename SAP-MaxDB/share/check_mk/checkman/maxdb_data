title: SAP MaxDB: Data
agents: linux
catalog: app/maxdb
license: GPL
distribution: check_mk
description:
 This check checks the use of the database. Threshold values can also be specified as a percentage and as absolute values. These can be defined via the setup, the default is a percentage value of 80/90 percent for WARN/CRIT.
 In addition to this, there is a check for BadIndexes and BathVolumes, which can also be configured in the setup.

 The agent plugin MaxDB is required for this check.

item:
 Database

discovery:
 One service is created for each Database

